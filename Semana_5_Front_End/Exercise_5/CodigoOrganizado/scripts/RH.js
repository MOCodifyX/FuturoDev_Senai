document.getElementById("button_Funcionarios_Info").addEventListener("click", function() {
    var table = document.getElementById("Funcionarios_Info");

    // Alterna a classe "hidden" para mostrar ou esconder a tabela
    table.classList.toggle("hidden");
});